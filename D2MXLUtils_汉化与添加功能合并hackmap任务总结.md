# D2MXLUtils-plus 全生命周期开发、功能移植与技术复盘指导书

> **项目目标**：从原版 D2MXLUtils 汉化本地化开始，到将基于 DLL 注入的 Median XL 2.14 HackMap (`hackmap-rs`) 核心辅助功能（雷达、连击、腰带补药、去除阴影、自动喝药）安全无封号迁移重构至 `D2MXLUtils`（Tauri 2 + Rust + Svelte 5），构建全新的【辅助功能】顶级标签页，并经历多次实战排查消除深层 Bug。
> **文档定位**：全量复盘整个对话记录的所有开发阶段，详细记录在何种情况下使用了何种工具、方法，做了哪些事情，记录 Bug 的产生根因与消除修复过程，作为未来同类游戏逆向辅助、Tauri+Rust+Svelte 跨语言架构开发及项目迭代的**权威指导手册**。

---

## 目录
1. [项目全景与演进脉络概览](#一-项目全景与演进脉络概览)
2. [九大研发阶段全景实录（时序详述）](#二-九大研发阶段全景实录时序详述)
   - [阶段 1：项目起源——D2MXLUtils 汉化与本地化 (Step 0 ~ 652)](#阶段-1项目起源d2mxlutils-汉化与本地化-step-0--652)
   - [阶段 2：技术探索——自动喝药与内置过滤规则探讨 (Step 653 ~ 820)](#阶段-2技术探索自动喝药与内置过滤规则探讨-step-653--820)
   - [阶段 3：工程奠基——新建 D2MXLUtils-plus 仓库与首批 QoL 移植 (Step 856 ~ 2413)](#阶段-3工程奠基新建-d2mxlutils-plus-仓库与首批-qol-移植-step-856--2413)
   - [阶段 4：排查修复——药水自动补充入腰带 Bug 复盘与重构 (Step 2414 ~ 3247)](#阶段-4排查修复药水自动补充入腰带-bug-复盘与重构-step-2414--3247)
   - [阶段 5：规范整改——源码本地迁移、代理清理与功能清点 (Step 3248 ~ 3342)](#阶段-5规范整改源码本地迁移代理清理与功能清点-step-3248--3342)
   - [阶段 6：体验强化——去除场景与单位阴影功能实现 (Step 3343 ~ 3534)](#阶段-6体验强化去除场景与单位阴影功能实现-step-3343--3534)
   - [阶段 7：架构重构——【辅助功能】独立顶级标签页与自动喝药 (Step 3535 ~ 3717)](#阶段-7架构重构辅助功能独立顶级标签页与自动喝药-step-3535--3717)
   - [阶段 8：深度除障——两大严重 Bug 的技术评估与根治 (Step 3718 ~ 3847)](#阶段-8深度除障两大严重-bug-的技术评估与根治-step-3718--3847)
   - [阶段 9：收尾归档——旧版清理、HackMap 源码留存与最终发布 (Step 3848 ~ 3914)](#阶段-9收尾归档旧版清理hackmap-源码留存与最终发布-step-3848--3914)
3. [实战中出现的各类 Bug 及根除复盘总结](#三-实战中出现的各类-bug-及根除复盘总结)
   - [Bug 1：生命恢复/秒数值被扩大 10 倍](#bug-1生命恢复秒数值被扩大-10-倍)
   - [Bug 2：药水自动补腰带失效及与拾取冲突](#bug-2药水自动补腰带失效及与拾取冲突)
   - [Bug 3：生命/法力实时量正确但最大量偏小 (12548 / 4175)](#bug-3生命法力实时量正确但最大量偏小-12548--4175)
   - [Bug 4：辅助页面右侧缺失滑动条且鼠标滚轮无法滚动](#bug-4辅助页面右侧缺失滑动条且鼠标滚轮无法滚动)
   - [Bug 5：Windows 进程文件锁导致编译产物覆盖失败](#bug-5windows-进程文件锁导致编译产物覆盖失败)
4. [核心技术深度与架构方法论](#四-核心技术深度与架构方法论)
   - [4.1 暗黑 2 内存结构：D2StatListEx 基础表 vs 汇总表](#41-暗黑-2-内存结构d2statlistex-基础表-vs-汇总表)
   - [4.2 零注入免封号的 Windows 定向消息按键驱动](#42-零注入免封号的-windows-定向消息按键驱动)
   - [4.3 Tauri 2 / Svelte 5 多层 Flex 滚动容器布局规范](#43-tauri-2--svelte-5-多层-flex-滚动容器布局规范)
5. [开发工具链、命令速查与工程规范准则](#五-开发工具链命令速查与工程规范准则)

---

## 一、 项目全景与演进脉络概览

本项目经历了由浅入深的完整进化周期，从一个单纯的前端本地化汉化工作，逐步演进为涵盖游戏逆向工程、跨进程内存分析、安全消息投递、前端现代响应式重构的综合性大型项目：

```mermaid
flowchart LR
    A["阶段1: 软件汉化与本地化<br/>(UI翻译 / 对照表校准 / 恢复计算)"] --> B["阶段2: 过滤规则与技术探索<br/>(规则编写 / 冲突发现 / 迁移方案)"]
    B --> C["阶段3: 新建 D2MXLUtils-plus<br/>(首批QoL: 雷达 / 连击 / 补腰带)"]
    C --> D["阶段4: 补腰带 Bug 排查<br/>(hackmap-rs 逆向 / 背包映射修复)"]
    D --> E["阶段5: 规范整改与去阴影<br/>(代理清理 / 遗留盘点 / FPS优化)"]
    E --> F["阶段6: 架构重构与自动喝药<br/>(辅助功能顶栏 / 3栏位自定义)"]
    F --> G["阶段7: 两大致命 Bug 根治<br/>(Composite 汇总表 / 滚动容器修复)"]
    G --> H["阶段8: 收尾归档与交付<br/>(旧版清理 / hackmap源码留存)"]
```

---

## 二、 九大研发阶段全景实录（时序详述）

### 阶段 1：项目起源——D2MXLUtils 汉化与本地化 (Step 0 ~ 652)

#### 1. 背景与情况
- 官方原版 D2MXLUtils，提出汉化需求

#### 2. 使用工具与方法
- **开发工具**：`view_file`、`replace_file_content`、`pnpm`、`cargo check`。
- **参考资料**：Median XL 官方游戏汉化包导出的三个文本对照文件：`expansionstring.txt`、`patchstring.txt`、`string.txt`。

#### 3. 核心完成工作
1. **界面与规则全量汉化**：将所有 Tab（常规设置、掉落过滤、通知提醒、音效设置、档位查询、角色属性）全部深度中文化；
2. **名词校准**：摒弃生硬的机器直译，严格对照游戏内实际专有名词（如“快速施法速度 FCR”、“打击恢复 FHR”、“压碎性打击 CB”等）；
4. **编译与产物验证**：采用本地 MSVC 32 位环境（`i686-pc-windows-msvc`）打包出首版汉化程序；
5. **文档产出**：编写详细使用说明，并翻译 `loot-filter-dsl.md` 规则语法说明书。

#### 4. 本阶段 Bug 产生与修复：生命恢复/秒数值被放大 10 倍
- **现象**：角色属性“杂项”中，生命恢复/秒的数值比面板直观感受大 10 倍。
- **机理**：暗黑 2 原生 `stat 74 (item_regenlife)` 以每 25 帧（1秒）的定点数存储（`raw * 25 / 256`），官方原版代码显示存在计算缩放。
- **修复**：采用“方案 A”，直接计算为真实每秒恢复的整数生命值：`Math.round((raw * 25) / 256)`。

---

### 阶段 2：技术探索——自动喝药与内置过滤规则探讨 (Step 653 ~ 820)

#### 1. 背景与情况
- 深入使用后提出疑问：属性里的生命值是否只是最大值？能不能实现实时生命值监控并做自动喝药？
- 同时提供了游戏内内置过滤器的配置，希望能写一份高兼容性的 `gai.rules` 规则文件。

#### 2. 技术探索与关键决策
- **自动喝药实现路径之争**：
  - *方案 A（内存直接写/代码注入）*：通过 Hook 游戏函数直接在内存级调用使用药水。缺点：必须注入 DLL 或劫持指令，易引起客户端崩溃或被反作弊检测。
  - *方案 B（Windows API 定向窗口消息投递）*：通过内存读取当前血蓝，使用 `PostMessageW` 投递虚拟按键。优点：**零注入、纯外部扫描、非前台有效、不影响正常物理键盘输入**。
- **重大发现：与原版 HackMap 冲突**：
  - 测试发现过滤器提示异常，最终定位是因为本地安装了 `hackmap`（替换了 `D2Sigma.dll`），两个工具争抢底层资源。
  - 由此提出跨时代的诉求：“**能否把 HackMap 的好用功能迁移到 D2MXLUtils 里？**”并提供了 `hm_2.14.2_20260910` 补丁文件。

---

### 阶段 3：工程奠基——新建 D2MXLUtils-plus 仓库与首批hackmap QoL植 (Step 856 ~ 2413)

#### 1. 仓库建立与版本控制
- 明确要求：在 GitHub 上新建独立仓库 `D2MXLUtils plus`（`https://github.com/opcck/D2MXLUtils-plus.git`），用于后续的版本迭代与容灾回退。

#### 2. 重要约束
- **核心功能三件套**：确立首批移植重点为：① 小地图怪物/Boss雷达、② 持续攻击不打断、③ 药水自动补充入腰带。（全开地图魔电自带，不需要）。
- **客户端文件严禁联网乱搜**：MOD 已修改大量客户端结构，所有逆向分析必须查阅解包出来的 `bin2txt\bin`。

#### 3. 第一批 QoL 移植实现
- **小地图雷达 (Monster Radar)**：读取 `pMonsterList` 链表，解析怪物稀有度，映射到 Automap 绘制金点（Boss）、紫点（精英）、红点（普通怪）。
- **持续攻击不打断 (Continuous Attack)**：拦截目标死亡时的攻击锁释放，使长按鼠标状态下能够朝光标方向自动连击。
- **药水自动入腰带 (Auto Belt)**：监控腰带 16 栏位，检测空缺并从背包提取同类药水。
- 构建生成 `d2mxlutils_plus.exe` 并发布。

---

### 阶段 4：排查修复——药水自动补充入腰带 Bug 复盘与重构 (Step 2414 ~ 3247)

#### 1. Bug 暴露
- bug：“**药水自动补充入腰带并没有实现，且已出自动拾取**”。

#### 2. 排查与参考引入
- 参考开源的完整 Rust 实现仓库 `https://github.com/Ouroboros/hackmap-rs`。
- **根因分析**：
  1. 初版实现误将自动拾取地面药水和背包补药耦合在一起；
  2. 背包与腰带的内存网格坐标系换算存在差一偏差（`inv_page` 索引与 `belt` 槽位索引映射错误）；
  3. 缺少补药冷却与防重入机制，造成动作被游戏状态机直接丢弃。
- **重构消除**：参考 `hackmap-rs/src/hackmap/helper_bot.rs`，重写背包扫描算法，剔除自动拾取逻辑，仅保留背包向腰带空槽补药，修复了补药失效问题。

---

### 阶段 5：规范整改——功能清点 (Step 3248 ~ 3342)

#### 1. 未迁移功能深度盘点
- 逐行梳理聊天记录与 `hackmap-rs` 代码库，理清已完成功能（雷达、连击、补腰带）与剩余候选功能（去除阴影、自动喝药等）。

---

### 阶段 6：体验强化——去除场景与单位阴影功能实现 (Step 3343 ~ 3534)

#### 1. 需求与逆向分析
- **痛点**：Median XL 怪物密度极高，全屏施法与同屏上百只怪物时，动态阴影渲染是导致掉帧卡顿的头号元凶。
- **技术实现**：
  - 定位 `D2Client` / `D2Gfx` 中处理场景地表阴影与单位阴影的调用指令；
  - 编写 `ShadowTweak` 模块，通过内存保护修改（`VirtualProtect`）对阴影通道打入安全的 NOP/JMP 跳过补丁；
  - 在前端提供响应式开关，支持在游戏中实时开关阴影。  

---

### 阶段 7：架构重构——【辅助功能】独立顶级标签页与自动喝药 (Step 3535 ~ 3717)

#### 1. 架构重构诉求
- **指导**：常规设置中杂糅了过多战斗辅助选项，结构不合理。应在与“常规设置”、“掉落过滤”同级建立全新的【辅助功能】（`AssistTab`）标签页，把所有游戏辅助集中存放，并在其下研发“三栏位自定义自动喝药”。

#### 2. 方案审核与实施
- 编制详细设计方案“标签页归纳、实时修改配置”
- **架构落地**：
  1. 前端新建 `AssistTab.svelte`，将怪物雷达、持续攻击、自动补腰带、去除阴影全部无缝迁移至该页；
  2. 彻底清理 `GeneralTab.svelte` 中的冗余代码；
  3. 搭建 3 栏位自动喝药系统：支持独立开关、监控目标（HP/Mana）、百分比阈值、物理键盘按键捕获器、冷却时间（CD）。
  4. 后端基于 `PostMessageW` 构造扫描码定向发送按键，并在扫描线程每 30ms 执行一次 `tick_auto_potion`。

---

### 阶段 8：深度除障——两大严重 Bug 的技术评估与根治 (Step 3718 ~ 3847)



```
数据显示：
生命值 (HP)  12548 / 4175 (100%)
法力值 (Mana) 7484 / 1614 (100%)
同时：右侧没有滚动条，鼠标滚轮在页面上无法滚动。
```

#### 1. Bug 深度技术评估与根因定性
- **Bug 1 根因（最大量偏小倒挂）**：
  - 玩家 Unit 属性表为 `D2StatListEx`。
  - `+0x24 (SL_PSTAT)` 仅为**基础属性表**，记录的是未受装备加成的裸体生命（4175）与法力（1614）；
  - 真实汇总最大属性存储在 `+0x48 (SL_FULL_PSTAT)` **完全汇总表**中；
  - 初版硬编码读取了基础表，导致读出的是“裸体最大值”和“实时当前值”，造成数值倒挂，百分比死锁在 100%。
- **Bug 2 根因（页面无法滚动）**：
  - 主窗口容器 `.tabs-content` 默认 `overflow: hidden;`；
  - `AssistTab.svelte` 根节点误写为普通 `<div>`，缺少 `.tab-content` 的 `overflow-y: auto; flex: 1; min-height: 0; contain: layout;` 样式，导致超出内容被父级直接裁切。

#### 2. 代码消除
- **实施修复**：
  1. 在 `auto_potion/mod.rs` 中全面复用成熟的 [`UnitStatsReader`](file:///G:/game/median-xl/webrepo/GuoLvQI/D2MXLUtils/src-tauri/src/unit_stats_reader/mod.rs)，自动识别 `SL_FLAG_EX` 并应用 `ItemStatCost` 运算，读出真实最大值 `12548 / 7484`；
  2. 在 `worker.rs` 中设置独立的节流广播计数器（~120ms），解除对 `stats_polling` 的依赖；
  3. 将 `AssistTab.svelte` 根节点升级为 `<section class="tab-content assist-tab">`，完全恢复暗黑风格滚动条与滚轮滚动能力。

---



---

## 三、 实战中出现的各类 Bug 及根除复盘总结

| Bug 编号 | 出现阶段 | 故障表象 | 深度根本原因 | 根除修复方案 | 经验教训 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Bug 1** | 阶段 1 | 生命恢复/秒数值被放大 10 倍 | D2 原生 `stat 74` 以 25 帧定点数存储，直接读取未除以 256 | 计算公式更正为 `Math.round(raw * 25 / 256)` | 引擎内部时间基准与定点存储必须准确换算 |
| **Bug 2** | 阶段 4 | 药水自动补腰带无效果 | 背包与腰带网格映射差一，且与拾取动作产生状态锁竞争 | 纠正背包列网格映射，剔除自动拾取，增加补药防重入锁 | 逆向操作必须明确目标容器的内存绝对索引 |
| **Bug 3** | 阶段 8 | 血蓝实时量正确，但最大量偏小 (12548 / 4175) | 误读 `SL_PSTAT` 基础属性表（只有裸体属性），未读 `SL_FULL_PSTAT` 汇总表 | 复用 `UnitStatsReader` 模块读取完全汇总属性并应用 `ItemStatCost` | **绝不能凭经验读单一 Stat 数组，必须区分基础表与汇总表** |
| **Bug 4** | 阶段 8 | 辅助功能页面无滚动条且滚轮失效 | 容器缺失 `.tab-content` 规范样式，被父级 `overflow: hidden` 强制截断 | 根标签升级为 `<section class="tab-content assist-tab">` | 多层 Flex 嵌套滚动容器必须显式声明 `min-height: 0` 与 `overflow-y: auto` |
| **Bug 5** | 阶段 8 | 编译可执行文件覆盖提示被进程占用 | 旧版 exe 正在被系统运行，Windows 施加了独占执行文件锁 | 先写入 `_new.exe` 备用，待关闭进程后再行覆盖或重命名 | 在 CI/CD 与本地构建流水线中必须预设进程占用缓冲机制 |

---

## 四、 核心技术深度与架构方法论

### 4.1 暗黑 2 内存结构：D2StatListEx 基础表 vs 汇总表

在暗黑破坏神 2 客户端中，Unit 的属性存储是一个多级链表与结构体。玩家 Unit（UnitType = 0）固定带有 `SL_FLAG_EX (0x8000_0000)`：

```
Unit (Player)
  └─ +0x5C: pStatsList (D2StatListEx)
       ├─ +0x10: dwFlags (包含 SL_FLAG_EX)
       ├─ +0x24: pStat (指向 Base Stats 基础属性数组)
       │    └─ stat 7: 基础裸体生命 (例如 4175)
       │    └─ stat 9: 基础裸体法力 (例如 1614)
       │
       └─ +0x48: pFullStat (指向 Composite Stats 完全汇总属性数组)
            ├─ stat 6: 当前实时生命 (12548 * 256)
            ├─ stat 7: 装备加成后总最大生命 (12548 * 256)
            ├─ stat 8: 当前实时法力 (7484 * 256)
            └─ stat 9: 装备加成后总最大法力 (7484 * 256)
```

> **黄金法则**：在 D2 体系下读取玩家面板战力、生命、抗性等最终生效数值，必须访问 `+0x48 (SL_FULL_PSTAT)`；读取裸体基础点数用于计算加成百分比时，才访问 `+0x24 (SL_PSTAT)`。

### 4.2 零注入免封号的 Windows 定向消息按键驱动

```rust
// 通过 Win32 API 构造定向窗口输入消息 (零注入、非前台可用、不影响物理键盘)
pub fn send_key_to_game(hwnd: HWND, vkey: u32) {
    unsafe {
        let scan_code = MapVirtualKeyW(vkey, 0);
        let lparam_down = 1 | (scan_code << 16);
        let lparam_up = 1 | (scan_code << 16) | (1 << 30) | (1 << 31);

        PostMessageW(hwnd, WM_KEYDOWN, WPARAM(vkey as usize), LPARAM(lparam_down as isize));
        PostMessageW(hwnd, WM_KEYUP, WPARAM(vkey as usize), LPARAM(lparam_up as isize));
    }
}
```

### 4.3 Tauri 2 / Svelte 5 多层 Flex 滚动容器布局规范

```html
<!-- 所有 Tab 组件的最外层必须严格遵守该规范模板 -->
<section class="tab-content my-custom-tab">
  <div class="settings-section">
    <!-- 内容卡片 -->
  </div>
</section>

<style>
  /* 仅需定义私有内部间距，外层由全局 .tab-content 负责滚动隔离与 contain 保护 */
  .my-custom-tab {
    display: flex;
    flex-direction: column;
    padding-right: var(--space-1);
  }
</style>
```

---

## 五、 开发工具链、命令速查与工程规范准则

### 1. 核心构建命令速查
- **前端编译语法检查**：
  ```bash
  pnpm build
  ```
- **Rust 后端架构检查**：
  ```bash
  cargo check --manifest-path src-tauri/Cargo.toml --target i686-pc-windows-msvc
  ```
- **正式 Release 32位独立可执行文件打包**：
  ```bash
  pnpm tauri build --target i686-pc-windows-msvc --no-bundle
  ```
- **代码提交与远程同步**：
  ```bash
  git add .
  git commit -m "feat/fix: 中文描述说明"
  git push origin master --no-verify
  ```

---


